git 上传命令
git config --global user.name “name”
git config --global user.email youxiang@qq.com

git init　　　　　　　　　　　　　　（初始化空间会在当前的空间中生成一个.git  文件　可以通过　　ls -a　进行查看）
 
git status 　　　　　　　　　　　　（查看状态　没有什么用，但是也输入一遍吧）
 
git add wenjiandizhi　　　　　　　（指定要上传的代码的空间）
 
git commit -m "init files"　　　　（双引号的是注解　比如第几次上传的版本是多少可以自己定义）
 
git remote add origin https://github.com/.....　　（把传到的git 地址写上）
git push -u origin master　　　　（如果刚建的git仓库在master前加一个+（+master）推送上去就可以了）

安装好git之后一般会在C盘的C:\Users\Administator目录下生成 .gitconfig配置文件。用文档编辑工具打开该文件
添加：
[user]
name = wb-ly409739  //你的用户名
email = wb-ly409739@alibaba-inc.com  //你的git邮箱账号
[credential]
    helper = store
然后保存就可以了。
方法二：
直接在git bash 中执行命令：git config --global credential.helper store
在输入一次账号密码就可以保存了

linux 命令
创建文件（多）          sudo touch a.txt
创建文件夹（多）        sudo mkdir  a
删除文件（多）          sudo rm  a
删除空文件夹（多）       sudo rmdir a
删除带文件的文件夹（多）  sudo rm -r a a.txt
移动文件(多个不同的文件)  sudo mv  a a.txt   /...地址
复制文件(只是文件)       sudo cp  a   /...地址
复制文件(多个不同文件)    sudo cp -r  a a.txt   /...地址
sudo apt-get update 
sudo apt-get upgrade
sudo apt install 
